# PC 간 공유 설정 (구글시트 연동)

여러 PC가 같은 데이터를 보고 함께 수정하려면, 무료 **구글시트 + Apps Script 웹앱**을 공용 저장소로 사용합니다.
한 번만 설정하면 됩니다.

## 1. 구글시트 + 스크립트 만들기 (관리자 1회)

1. [구글 드라이브](https://drive.google.com)에서 **새 구글 시트** 하나 생성 (이름은 자유, 예: `고객DB`)
2. 메뉴 **확장 프로그램 → Apps Script** 클릭
3. 편집기의 기본 코드를 모두 지우고, 이 저장소의 **`google-apps-script.gs`** 내용을 전부 붙여넣기 → 저장(💾)
4. 오른쪽 위 **배포 → 새 배포** 클릭
   - 톱니바퀴(유형 선택) → **웹 앱**
   - **실행 주체: 나(본인 계정)**
   - **액세스 권한: 모든 사용자**
   - **배포** → 권한 검토/승인 (본인 계정으로 허용)
5. 생성된 **웹 앱 URL** (`https://script.google.com/macros/s/.../exec`) 을 복사

## 2. 앱에 연결

1. 고객 DB 앱 상단의 **☁ 동기화** 클릭
2. 복사한 URL 붙여넣기
3. **앱을 열 때 자동으로 동기화** 체크 (권장)
4. **지금 동기화** 클릭 → 내 데이터가 시트에 올라가고, 시트 데이터가 합쳐집니다

## 3. 다른 PC에서

- 같은 페이지(예: GitHub Pages 주소)를 열고 → **☁ 동기화** → **같은 URL** 입력 → **지금 동기화**
- 이제 두 PC가 같은 시트를 공유합니다. 변경 후 **동기화**를 누르면 서로 반영됩니다.

## 동작 방식 / 주의

- 동기화는 **시트와 내 데이터를 합칩니다**. 같은 사람(같은 id)이 양쪽에 있으면 **더 최근에 수정한** 내용을 남깁니다.
- 자동 충돌 합치기까지는 하지만, **삭제는 양방향 전파가 되지 않습니다**. (한쪽에서 지운 항목이 다른 PC 동기화 때 되살아날 수 있음)
  → 삭제는 가능하면 한 사람이, 또는 정리 후 모두가 한 번씩 동기화해 정렬하세요.
- 동시에 여러 명이 같은 항목을 수정하면 마지막 동기화가 우선합니다. 소규모 팀 사용에 적합합니다.
- 시트의 `DB` 탭은 사람이 직접 봐도 됩니다(열: id·회사·이름·… ). 직접 편집도 가능하지만, 가급적 앱으로 수정하세요.

## 더 견고한 공유가 필요하면

실시간 동시편집·권한 관리·삭제 동기화가 중요해지면 Supabase 같은 전용 백엔드로 확장할 수 있습니다. 필요 시 요청하세요.
